import React from "react";

const BlueButton = ({ button }) => {
  return (
    <p className="text-[0.7vw] font-light bg-sky-100 py-[1.5vmin] px-[2vmin] rounded-full text-center uppercase text-sky-600">
      {button}
    </p>
  );
};

export default BlueButton;
